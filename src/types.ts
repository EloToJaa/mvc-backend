namespace Types {
	export type Difficulty = "easy" | "medium" | "hard" | "impossible";
	export type Credentials = string | undefined;
	export type LoginStatus = "success" | "failure";
}

export default Types;
